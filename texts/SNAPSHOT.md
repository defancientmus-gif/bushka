# SNAPSHOT — текущее состояние БУ.шки (факты, не желания)

> Источник правды о состоянии. Обновлять при деплое/изменении инфраструктуры.

## Прод
- Сайт: https://defancientmus-gif.github.io/bushka/
- Репо: `defancientmus-gif/bushka`. Деплой фронта: push в `main` → GitHub Actions → Pages (~1 мин).
- Версия: `APP_BUILD` в `apps/pwa/src/lib/version.ts` (метка `vNNN` у лого). **Бамп на каждую правку фронта.**

## Что живо (фронт)
- React + TS + Vite PWA. Рынок (вертикальные карточки 4:5, «Фильтр» в строке поиска, детальная карточка товара), Создать (живое sticky-превью, лента вложений с drag-reorder, видео-петли, авто-«%» АКБ), Профиль/ЛК (дашборд, деньги, склад).
- PWA: SW **cache-first** навигация (мгновенный старт), сторож свежести `version.json` (форс-релоад на новый билд).
- Хранение локально: localStorage (товары/профиль/сделки/избранное) + IndexedDB (видео-блобы).

## Двор (Supabase) — ПОДКЛЮЧЕН, ждёт двух настроек Жени
- Проект: `vnylpvmhelzlxusvydab.supabase.co`. Publishable-ключ — в `apps/pwa/src/lib/supabase.ts` (публичный, это норма).
- Модель **local-first**: источник правды — устройство; облако синхронит, отказоустойчиво (двор недоступен → тихо работаем локально).
- Таблица `listings` + RLS — миграция `supabase/migrations/0001_listings.sql` (читать живое — всем, писать своё — `owner = auth.uid()`).
- Вход: **анонимный** (устройство = личность, без логина). Email/телефон — позже (по рецепту Разберёмся: magic-link + Site URL).
- Синк: `store` пушит товар при сохранении/смене статуса/удалении; `MarketView` тянет чужие лоты `fetchMarket()` + склеивает с локальными + сид.
- supabase-js **вендорно** (npm-зависимость, бандлится Vite — без CDN).
- ⏳ ОТКРЫТО (за Женей): прогнать SQL-миграцию в SQL Editor + включить **Anonymous sign-ins** (Authentication → Sign In/Providers).

## Известные ограничения / риски
- Медиа сейчас едет в `jsonb` (base64-фото в строке) — ок для теста, **НЕ для масштаба** (тяжёлые строки/трафик). Milestone 2: фото/видео в Storage (URL в строке), тогда же — лёгкий кэш витрины.
- Видео-блобы локальны (не синкаются) — на чужом устройстве виден кадр-постер.
- Бесплатный Supabase засыпает после ~недели простоя → нужен cron-пинг (поставить).
- AI/секреты — ещё нет; когда появятся, только через Edge Functions (рецепт SETUP_GITHUB_SUPABASE).

## Следующее
1. Женя: SQL-миграция + anon-тумблер → я верифицирую round-trip живьём (анонимный вход → push лота → fetch витрины).
2. Тест с парой людей (видят товары друг друга).
3. Потом: медиа в Storage + кэш витрины; вход по email/телефону; cron-пинг; честный рейтинг.
